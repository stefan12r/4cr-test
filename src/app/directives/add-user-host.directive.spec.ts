import { AddUserHostDirective } from './add-user-host.directive';

describe('AddUserHostDirective', () => {
  it('should create an instance', () => {
    const directive = new AddUserHostDirective();
    expect(directive).toBeTruthy();
  });
});
